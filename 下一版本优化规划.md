# 测试用例阅读器 · 下一版本（v2.x）优化规划文档

> 角色视角：高级产品经理
> 依据：当前版本真实浏览器深度体验（餐饮Ros v2.10.0 报告 396 条用例 / 21 Sheet 实测）+ 代码现状梳理 + 市面主流测试管理工具（TestRail / Qase / Xray）设计模式研究
> 日期：2026-07-30

---

## 一、当前版本功能现状盘点

### 1.1 已具备的能力（本次实测确认）

| 能力域 | 现状 |
|---|---|
| 文件接入 | 初始页上传/选择历史文件，弹性表头识别（外置 JSON 别名配置），自动区分可执行 Sheet 与只读预览 Sheet（矩阵/信息两种预览形态） |
| 用例执行 | 卡片式逐条执行：前置条件/步骤/预期/附加信息 + 通过/失败/阻塞/跳过 四态标记 + 实际现象/发现时间/BugID/测试人员/备注 五个记录字段 |
| 视图体系 | 卡片视图、表格视图（行内标结果）、测试详情（Sheet 分组总览）、只读 Sheet 预览 |
| 测试轮次 | 顶栏结果列选择器（如 rc9/rc10/rc11），＋新建列开启新一轮；按选中列统计进度；缺失列黄色警示回退 |
| 用例筛选 | 七维组合筛选（关键词/编号范围/状态/参照列/优先级/模块/Sheet/风险），全局工作集收窄，localStorage 预设 + 内置冒烟/复测预设 |
| 快捷键 | ←/→ 切换用例、1/2/3/4 标记结果、Esc 关闭面板（仅执行视图生效） |
| 富内容 | 阅读侧栏、图片灯箱（左右切图） |
| 数据安全 | Excel 为唯一数据源，openpyxl 原子写回 + 自动备份，写入精确落行列 |

### 1.2 本次深度体验发现的问题清单（按严重度排序）

| # | 严重度 | 问题 | 实测证据 |
|---|---|---|---|
| P-1 | 高（Bug） | 顶栏模块下拉选择只读 Sheet（如"问题类型"）后**不跳转预览且下拉变空白**，用户会以为功能坏了 | 实测选择"问题类型"后页面停留在表格视图，下拉框显示为空 |
| P-2 | 高 | 顶栏 9 个控件在 1536px 宽度下**换行成两排**，"下载 Excel"孤悬第二行；信息架构过载且无分组 | 执行页截图可见换行 |
| P-3 | 高 | 表格视图"编号"列：当文件无独立 ID 列时回退为标题，导致**编号列与标题列内容重复**，且窄列强制换行 4 行，一屏只能看 2 条用例 | ros测试整机用例 Sheet 实测 |
| P-4 | 中 | 进度条只有"已完成 x/y"，**没有 PASS/FAIL/BLOCK/NA 分布**，测试负责人无法一眼判断质量状态（366/396 完成，但失败多少？） | 实测进度区 |
| P-5 | 中 | 测试详情页 Sheet 排序：**只读预览 Sheet 排在可执行组之前**，打开先看到 3 个"只读预览"，核心执行组反而要下滚 | 实测详情页 |
| P-6 | 中 | 筛选面板模块 chips 过多时（24 个占 4 行）面板高度失控，无折叠/搜索 | 餐饮Ros 文件实测 |
| P-7 | 中 | 快捷键**零可见性**：界面无任何提示，新用户完全不知道 1/2/3/4 可标记结果 | 代码确认仅 keydown 绑定，无帮助入口 |
| P-8 | 中 | 表格视图 197 行整表渲染，无排序、无虚拟滚动；396 条文件切视图有可感知卡顿 | 实测 + 代码确认 |
| P-9 | 低 | 误标记无撤销：标错结果只能手动改回，且发现时间等联动字段可能已被覆盖 | 代码确认无 undo 栈 |
| P-10 | 低 | 初始页填写的"测试阶段/测试人员"与执行过程弱关联：卡片内测试人员仍需手填 | 实测初始页与卡片字段 |
| P-11 | 低 | BugID 为纯文本，无格式校验、无链接跳转（如 Jira/禅道 URL 模板） | 实测卡片字段 |

> **P-1 复核结论（2026-07-31）**：真实浏览器手动复现无法重现——鼠标选择只读 Sheet 可正确跳转预览且下拉保持选中。当时"不跳转且下拉空白"系自动化测试脚本将下拉 value 误设为显示文本（非法值）所致（`selectedIndex=-1` 假象）。产品功能无缺陷，P-1 从缺陷清单降级为"无需修复"，B1 相应关闭。

---

## 二、竞品设计模式研究（TestRail / Qase / Xray）

| 竞品模式 | 说明 | 对本项目的启示 |
|---|---|---|
| **执行仪表盘**（TestRail Run Overview / Qase Run Dashboard） | 每个测试轮次有独立仪表盘：环形图（各状态占比）、按模块堆叠条、通过率趋势 | 我们已有多轮次结果列数据，只差一个聚合展示层；纯前端可实现 |
| **快速标记 + 批量操作**（TestRail bulk edit、Qase express run） | 表格多选行批量标记结果；键盘流水线操作是 power user 生产力核心 | 表格视图增加多选 + 批量标记；补全快捷键体系并给予可见性 |
| **失败即登记**（Xray defect linking、Qase result wizard） | 标记 FAIL 时立即弹出缺陷信息快捷登记（备注必填引导 + 缺陷号），减少事后补录 | FAIL/BLOCK 时自动聚焦备注区并高亮 BugID 字段，形成"失败三件套"引导 |
| **轮次对比**（TestRail Runs comparison / milestone 趋势） | 跨轮次结果矩阵：一行一个用例，一列一轮结果，回归趋势一目了然 | 我们 results 字典已含全部轮次数据，可做纯前端对比视图 |
| **会话上下文**（TestRail assignee、Qase run configuration） | 测试人员/环境等会话信息一次配置全程生效 | 初始页"测试人员"应自动预填到每条用例的测试人员字段 |
| **命令面板 / 全局搜索**（现代工具普遍 Ctrl+K） | 键盘直达任意用例/功能 | 低成本高感知的效率功能，适合键盘重度的测试场景 |

**明确不借鉴**（与产品定位冲突）：账号体系、服务端数据库、用例编辑器、需求关联矩阵——本产品定位是"Excel 就地执行器"，轻量零迁移是核心竞争力，不做重型平台化。

---

## 三、第一性原理：测试人员的核心工作循环

测试执行者一天的真实循环是：**「看用例 → 做操作 → 记结果 → （失败时）记现象和缺陷号 → 下一条」**，循环数百次。
测试负责人的核心诉求是：**「随时知道测到哪了、挂了多少、挂在哪个模块、比上一轮好还是坏」**。

因此下版本的价值主轴只有两条：

1. **把单条循环的耗时和摩擦压到最低**（执行者视角）——快捷键、批量、自动填充、失败快捷登记；
2. **把整体质量状态的可见性拉到最高**（负责人视角）——状态分布、仪表盘、轮次对比、一键报告。

所有需求按"是否服务这两条主轴"取舍。

---

## 四、下版本优化清单

优先级定义：P0=下版本必做；P1=下版本力争；P2=后续版本。工作量：S≤0.5天 / M≤2天 / L≤5天。

### A. 界面布局改进

| 编号 | 优先级 | 工作量 | 优化项 | 方案要点 |
|---|---|---|---|---|
| A1 | P0 | M | **顶栏信息架构重组** | 按"视图切换 / 轮次与筛选 / 全局动作"三组重排：左侧放视图组（执行/表格/详情），中间放工作集组（结果列、筛选、模块跳转、下一条未测），右侧收纳组（下载 Excel、更换用例收进"···"更多菜单）；保证 1366px 宽度单行不换行 |
| A2 | P0 | S | **表格视图编号列去重** | 当 caseId 与标题相同（无独立 ID 列）时隐藏编号列或显示行号；编号列设置 max-width + 省略号，悬停显示全文 |
| A3 | P0 | S | **详情页 Sheet 排序调整** | 可执行组置顶（按原 Sheet 顺序），只读预览组折叠收在"参考资料"分区尾部 |
| A4 | P1 | S | **筛选面板 chips 折叠** | 每组默认最多显示 2 行，超出显示"展开全部 (24)"；模块组内增加快速过滤输入框 |
| A5 | P1 | M | **进度条升级为状态分布条** | 单进度条改为四色堆叠条（绿=通过/红=失败/黄=阻塞/灰=跳过+未测），悬停显示各状态数量；保持现有配色体系 |
| A6 | P2 | M | **深色模式** | CSS 变量化现有色板，跟随系统 prefers-color-scheme + 手动开关 |

### B. 交互体验优化

| 编号 | 优先级 | 工作量 | 优化项 | 方案要点 |
|---|---|---|---|---|
| B1 | P0 | S | **修复只读 Sheet 跳转 Bug（P-1）** | 模块下拉选中只读 Sheet 时正确进入预览视图，且下拉保持选中态显示 |
| B2 | P0 | S | **快捷键可见性** | 顶栏"?"入口 + 按 ? 键弹出快捷键速查浮层；卡片结果按钮上已有 1/2/3/4 角标，补齐 ←/→/N 等提示 |
| B3 | P0 | M | **失败快捷登记流** | 标记 FAIL/BLOCK 时：自动滚动到记录区、聚焦"实际现象"输入框、发现时间自动填当前时间、BugID 字段高亮提示；PASS/NA 不打扰 |
| B4 | P0 | S | **测试人员自动填充** | 初始页"测试人员"存入会话（localStorage），标记结果时若卡片测试人员为空则自动填入；可手动覆盖 |
| B5 | P1 | M | **撤销最近标记** | 内存撤销栈（最近 10 步），Ctrl+Z 撤销结果标记及联动字段写入；标记后 toast 提示"已标记 通过 · 撤销" |
| B6 | P1 | M | **表格批量标记** | 表格视图行首复选框 + 全选当前 Sheet；底部浮出批量操作条：批量 PASS/NA（失败/阻塞不允许批量，强制逐条留痕）；批量操作走既有 PATCH 队列逐条落盘 |
| B7 | P1 | S | **快捷键扩展** | N=下一条未测、F=开合筛选、V=切换卡片/表格、Ctrl+Z=撤销；全部仅在非输入态生效，与现有约定一致 |
| B8 | P2 | M | **命令面板（Ctrl+K）** | 全局模糊搜索用例标题/编号直达 + 常用动作（切轮次/应用预设/跳 Sheet）；复用筛选的关键词匹配逻辑 |
| B9 | P2 | S | **BugID 链接化** | 设置项配置缺陷系统 URL 模板（如 `https://jira.xxx.com/browse/{id}`），BugID 显示为可点击链接；纯前端配置存 localStorage |

### C. 新增实用功能

| 编号 | 优先级 | 工作量 | 功能 | 方案要点 |
|---|---|---|---|---|
| C1 | P0 | L | **轮次执行仪表盘** | 测试详情页顶部新增仪表盘区：当前轮次环形图（四态占比）、按模块 TOP 失败条形图、按 Sheet 完成度列表；数据全部来自已有 /api/cases 负载，纯前端聚合，零后端改动 |
| C2 | P1 | L | **多轮次对比视图** | 新增"轮次对比"入口：行=用例、列=各结果列（rc9/rc10/rc11…），单元格四色块；提供"仅看回归恶化项"（上轮 PASS 本轮 FAIL）一键过滤——直接复用筛选工作集机制 |
| C3 | P1 | M | **一键测试报告** | 基于仪表盘数据生成报告：总体统计 + 各模块通过率 + 失败用例清单（标题/现象/BugID/人员）；导出为剪贴板 Markdown 与打印友好页（浏览器打印→PDF），不改 Excel |
| C4 | P1 | S | **断点续测** | localStorage 记住每个文件最后停留的用例与轮次列，重新打开同一文件时提示"继续上次进度（第 x 条）？" |
| C5 | P2 | M | **执行计时统计** | 会话内记录每条用例从打开到标记的耗时，报告中输出本轮总用时与平均单条用时；不写入 Excel |
| C6 | P2 | M | **附件截图粘贴** | 备注区支持 Ctrl+V 粘贴截图，存到服务端 uploads/attachments 并在备注写入引用标记；需评估 Excel 内嵌入 or 旁路存储的取舍后再立项 |

### D. 性能提升

| 编号 | 优先级 | 工作量 | 优化项 | 方案要点 |
|---|---|---|---|---|
| D1 | P1 | M | **表格视图虚拟滚动** | 超过 100 行时只渲染可视窗口 ±20 行；无第三方依赖，手写窗口计算，滚动条高度按总行数虚拟 |
| D2 | P1 | S | **/api/cases 协商缓存** | 响应带 ETag（文件 mtime+size），前端 If-None-Match 命中 304 免重复解析 1.4MB 工作簿 |
| D3 | P2 | M | **写回队列合并** | 连续快速标记时后端按用例合并写请求（现有 flushSave 基础上增加服务端 50ms 合并窗口），降低大文件反复全簿保存的 IO |
| D4 | P2 | S | **大文件加载骨架屏** | 解析超过 1s 时显示 Sheet 级加载进度，替代白屏等待 |

---

## 五、一致性原则（所有改动的硬约束）

1. **操作一致性**：四态结果的语义、颜色（绿/红/黄/灰）、快捷键（1/2/3/4）在卡片、表格、批量条、仪表盘、对比视图中必须完全一致；新增快捷键沿用"非输入态才响应"的既有约定。
2. **UI 风格统一**：新组件一律复用现有设计语言——胶囊 chips、btn/btn-ghost 按钮、卡片式浅色面板、现有主色与状态色板；不引入新的 UI 框架或图标库。
3. **功能逻辑连贯**：筛选工作集是全局唯一口径——仪表盘、批量操作、轮次对比、报告生成默认都作用于当前筛选结果，并明确标注"（筛选中）"，与现有导航/进度口径保持同构。
4. **架构底线**：Excel 仍是唯一数据源；所有新功能优先纯前端实现；确需后端的（D2/D3/C6）不引入数据库，不破坏原子写回与备份机制；会话偏好类数据一律 localStorage。

---

## 六、版本切分建议

| 版本 | 主题 | 范围 | 预估总量 |
|---|---|---|---|
| **v2.1（本次规划核心）** | "把执行摩擦降到最低" | P0 全部：A1 A2 A3 B1 B2 B3 B4 + C1 仪表盘 | ~2 周 |
| **v2.2** | "让质量状态看得见" | C2 轮次对比、C3 一键报告、B5 撤销、B6 批量标记、B7 快捷键扩展、A4 A5、C4、D1 D2 | ~2.5 周 |
| **v2.3** | "锦上添花" | B8 命令面板、B9 BugID 链接、C5 计时、A6 深色模式、D3 D4、C6（先做技术评估） | 按需排期 |

## 七、验收度量

- **效率指标**：单条用例"标记 FAIL + 记录现象 + 填 BugID"的操作路径从当前 ≥6 次点击/滚动降至 ≤3 次；100 条冒烟用例纯标记流耗时下降 30% 以上。
- **可见性指标**：负责人打开测试详情页 5 秒内可回答"失败多少条、集中在哪个模块、比上轮好坏"三个问题。
- **质量门槛**：所有改动保持 `node --check` 通过 + 浏览器零 console 报错 + openpyxl 磁盘核对（写入行列精确、其余单元格零变化）+ 396 条真实大文件回归。
- **兼容门槛**：现有轮次模式、筛选预设、只读预览、备份机制行为不回退。

---

*附：本次体验截图存于项目根目录 pm_shot_01~06（初始页/执行页/表格/详情/筛选/只读跳转Bug现场），核对后可删除。*

---

## 八、产品规划复核与新增需求（2026-08-10）

### 8.1 已落地能力核销

当前代码已基本落地以下原规划项，后续不再作为“待开发”重复排期：

- **A2 / A3**：表格视图编号去重、可执行 Sheet 置顶、只读 Sheet 收入参考资料区。
- **B2 / B3 / B4**：快捷键速查、失败/阻塞快捷登记、测试人员会话预填。
- **C1**：当前轮次执行仪表盘，包含完成率、四态分布和失败集中模块。
- **团队协作基础链路**：子任务导出、结果预览、冲突识别、结果合入和 xlsx 下载。

后续迭代应从“继续增加零散控件”转向“完整管理一次测试活动”。

### 8.2 新增产品需求池

| 编号 | 优先级 | 工作量 | 优化项 | 产品方案与价值 |
|---|---|---|---|---|
| E1 | P0 | M | **测试活动上下文** | 除测试阶段/人员外，增加版本号、构建号、设备、系统环境、测试范围、负责人、开始/结束时间；上下文随轮次保存并进入报告，解决“结果属于哪次测试”无法追溯的问题。 |
| E2 | P0 | M | **备份恢复中心** | 将现有自动备份变成可见功能：查看备份时间/来源、恢复前预览、恢复指定版本、恢复后自动再备份。清除结果、合入结果、批量操作都必须可回退。 |
| E3 | P0 | M | **分发任务生命周期** | 当前分发主要记录“测试人员”列；新增任务编号、分配时间、截止时间、任务状态（待执行/执行中/待回传/待合入/已完成）和合入记录，支持负责人快速识别逾期及未回传任务。 |
| E4 | P0 | M | **Excel 解析预检** | 上传或选择文件后展示 Sheet 分类、用例数量、字段映射、被跳过内容和歧义警告；用户确认后再补充“实际现象/发现时间”等列，避免解析结果静默改变原文件。 |
| E5 | P0* | M | **多人协作模式边界** | 当前服务使用进程级工作文件状态。默认继续定位为“单机文件协作”；若支持局域网多人访问，必须改为浏览器会话级状态，并增加文件版本冲突、写入锁和导入会话隔离。*仅在开放网络访问时列为 P0。* |
| E6 | P1 | L | **轮次回归分析** | 在现有 C2 基础上增加“上轮通过、本轮失败”“连续失败”“本轮新增失败”“已修复回归”等视图，帮助负责人直接定位质量恶化项。 |
| E7 | P1 | M | **一键测试报告增强** | 在现有 C3 基础上纳入 E1 测试上下文、分发任务、模块通过率、失败详情、BugID 和未完成清单，支持 HTML、Markdown 和打印 PDF。 |
| E8 | P1 | M | **批量执行与大数据量体验** | 表格视图支持多选和批量 PASS/跳过；超过 100 条时启用虚拟滚动，并保留失败/阻塞逐条登记约束，兼顾效率和记录质量。 |
| E9 | P1 | S | **断点续测与保存可靠性** | 记住文件/轮次/筛选范围/最后用例；保存失败时保留待提交变更并提供重试，避免网络波动或页面关闭造成现场记录丢失。 |
| E10 | P2 | M | **失败证据与缺陷联动** | 支持粘贴截图、复现步骤、设备状态、缺陷链接和责任人；先做旁路附件存储，不直接改写 Excel 图片结构。 |
| E11 | P2 | S | **文件库管理** | 增加文件搜索、最近使用、归档、重命名、删除确认和版本提示，避免 testing 目录文件数量增长后难以维护。 |
| E12 | P2 | S | **BugID 链接与配置** | 通过本地配置缺陷系统 URL 模板，自动把 BugID 显示为可点击链接；不引入账号体系或服务端数据库。 |

### 8.3 版本切分补充

在原 v2.1～v2.3 切分基础上，新增需求按以下方式归并，具体以本节优先级为准：

| 版本 | 主题 | 新增范围 |
|---|---|---|
| **v2.1** | **可靠执行与可恢复** | E1 测试活动上下文、E2 备份恢复中心、E4 解析预检、E9 保存可靠性；若产品开放局域网访问，同时提前实施 E5。 |
| **v2.2** | **质量管理与协作闭环** | E3 分发任务生命周期、E6 轮次回归分析、E7 一键报告增强、E8 批量执行与大数据量体验。 |
| **v2.3** | **证据沉淀与轻量集成** | E10 失败证据、E11 文件库管理、E12 BugID 链接；继续评估附件长期存储方案。 |

### 8.4 产品边界决策

1. **单机模式是默认产品形态**：Excel 仍是唯一数据源，测试结果原地写回，不引入账号、数据库和完整测试管理平台。
2. **团队协作当前定义为“文件协作”**：通过导出子任务、回传文件和预览合入完成协作，不宣称实时多人编辑。
3. **局域网多人访问属于独立能力**：一旦开放给多个浏览器用户，E5 必须优先于更多界面优化，先解决会话隔离和并发写入安全。
4. **所有破坏性操作可恢复**：清除、合入、批量写回、轮次初始化必须先备份，并且用户能在界面完成恢复，不要求手工操作 uploads 目录。

### 8.5 新增验收指标

- **可追溯性**：任意结果可回答“哪个版本、哪台设备、哪个环境、哪个测试活动、谁执行”。
- **可恢复性**：用户在 30 秒内能找到并恢复指定历史版本，恢复前后均有备份。
- **协作闭环**：每个已分发任务都有明确状态，负责人能识别未回传、逾期和未合入任务。
- **解析可信度**：上传预检能展示所有被跳过 Sheet、字段歧义和自动补列行为，不允许静默改变用户原文件。
- **效率指标**：100 条用例批量执行不卡顿；保存失败时现场输入不丢失；失败用例从标记到形成可交接记录不超过 3 次主要操作。

---

## 九、v2.1 开发方案（可靠执行与可恢复）

### 9.1 版本目标

v2.1 不继续扩展测试管理平台能力，目标是让一次测试活动具备完整的“可识别、可预检、可执行、可恢复”闭环：

1. 用户知道当前结果属于哪个版本、设备、环境和测试轮次。
2. 用户在执行前知道 Excel 将被如何解析，原文件不会被静默修改。
3. 页面刷新、网络波动或服务异常后，现场输入不会丢失。
4. 清除、合入和写回错误后，用户可以从界面恢复历史版本。

### 9.2 v2.1 范围

| 编号 | 功能 | 本版本交付范围 | 不纳入本版本 |
|---|---|---|---|
| E1 | 测试活动上下文 | 版本号、构建号、设备、环境、范围、负责人、开始/结束时间；与结果轮次绑定并随报告使用 | 不做账号、权限和服务端数据库 |
| E2 | 备份恢复中心 | 备份列表、来源和时间展示；恢复前确认；恢复前再备份；恢复后自动刷新页面 | 不做单元格级差异合并 |
| E4 | Excel 解析预检 | Sheet 分类、用例数量、字段映射、跳过项、歧义项、自动补列清单；确认后才允许正式选择 | 不改变现有表头识别规则，不做 Excel 编辑器 |
| E9 | 断点续测与保存可靠性 | 文件/轮次/筛选/最后用例记忆；失败写回保留草稿、重试、关闭前提示 | 不做跨设备云端同步 |

### 9.3 数据与存储方案

#### A. 测试活动元数据

继续遵守“Excel 是唯一数据源”的约束，在主工作簿增加隐藏 Sheet：`_qoder_runs`。解析器统一跳过 `_qoder*` Sheet，避免元数据进入用例列表。

建议表头：

```text
runId | resultHeader | stage | tester | version | build | device |
environment | scope | startedAt | endedAt | status | updatedAt
```

- `runId` 使用 UUID 或时间戳生成，保证同一文件多轮测试可区分。
- `resultHeader` 与现有结果列绑定，避免只按列名推断活动。
- `scope` 保存筛选条件快照，而不是只保存当时的用例数量。
- 首次打开旧文件时不强制创建元数据 Sheet；用户开启或补充测试活动信息后再写入，降低兼容风险。

#### B. 备份恢复

沿用现有 `make_backup()` 和 `_atomic_save()`，新增服务层能力：

- `list_backups(path, backup_dir)`：仅返回当前源文件对应的备份，按时间倒序。
- `restore_backup(path, backup_path, backup_dir)`：恢复前先备份当前文件，再通过临时文件原子替换。
- 恢复完成后清除解析缓存、刷新当前文件指纹和页面负载。
- 备份文件名不能由前端直接拼接路径，必须按安全文件名从备份目录解析，防止路径穿越。

#### C. 解析预检

新增纯只读 `inspect_workbook(path)`，不能调用会自动补列或保存文件的解析路径。返回：

```text
fileName | fileSize | sheetCount | executableCaseCount |
sheets[] | fieldMappings[] | skippedSheets[] | warnings[] | changes[]
```

正式执行流程调整为：

```text
上传/选择文件 -> 解析预检 -> 用户确认 -> 正式加载并允许补列 -> 进入执行页
```

其中 `changes[]` 必须明确列出“将新增实际现象列/发现时间列”等写入动作；预检失败时不得修改源文件。

#### D. 保存队列与断点

前端增加轻量待保存队列，使用 `localStorage` 保存未成功提交的最小变更，不保存整个工作簿：

```text
key: 文件名 + 文件指纹 + 结果列 + sheet + rowIndex
value: fields + createdAt + retryCount
```

- 保存成功后删除对应草稿。
- 保存失败不回滚用户输入，显示“待重试”状态和重试入口。
- 页面重新打开同一文件时，先提示待提交数量，再允许逐条或全部重试。
- 草稿设置 7 天有效期，并提供“清除本机草稿”入口，避免测试数据长期留在浏览器。
- 断点位置单独保存：文件指纹、结果列、筛选快照、当前用例索引；文件变化后只提示用户确认，不直接跳转旧行号。

### 9.4 API 设计

| 接口 | 方法 | 用途 |
|---|---|---|
| `/api/workbook/preflight` | POST | 对上传或已存在文件执行只读预检，返回映射、警告和待写入变化 |
| `/api/backups` | GET | 查询当前文件可恢复的备份列表 |
| `/api/backups/restore` | POST | 按备份标识恢复，执行前自动留档 |
| `/api/runs` | GET/POST/PATCH | 查询、新建、更新测试活动上下文 |
| `/api/cases` | PATCH | 保持现有写回接口，增加文件指纹/客户端变更标识校验 |

接口约束：

- 所有写接口必须携带当前文件名和文件指纹；文件已被外部修改时返回 409，不允许静默覆盖。
- 恢复、清除、批量写回、结果合入统一使用备份策略。
- 错误响应必须区分“文件被占用、文件已变化、字段不存在、网络失败”，前端不能统一显示为“网络错误”。

### 9.5 前端页面调整

#### 初始配置页

- 选择文件后增加“解析预检”按钮或自动进入预检结果。
- 预检页展示文件基本信息、可执行用例数、Sheet 分类、字段映射和风险提示。
- 增加“测试活动信息”折叠区，填写版本、构建、设备和环境。
- 预检未确认前，禁用“开始测试”。

#### 执行页

- 顶栏显示当前版本/设备/结果轮次，过长内容使用悬停查看完整值。
- 保存状态增加三态：已保存、保存中、待重试。
- 页面顶部或更多菜单增加“待提交 N 项”和“重试全部”。
- 增加“备份与恢复”入口，恢复后明确提示页面将刷新。
- 重新进入文件时提示“继续上次进度”并显示轮次与筛选条件。

### 9.6 开发顺序与里程碑

| 阶段 | 内容 | 产出 | 预计工期 |
|---|---|---|---|
| 0 | 基线冻结 | 固定现有真实文件样本、记录当前 API/页面行为、确认备份回滚路径 | 0.5 天 |
| 1 | E4 解析预检 | `inspect_workbook`、预检接口、预检页面、旧文件兼容 | 2 天 |
| 2 | E1 测试活动上下文 | `_qoder_runs` schema、活动配置 UI、结果轮次绑定、负载回显 | 1.5 天 |
| 3 | E2 备份恢复 | 备份查询/恢复 API、恢复确认弹窗、缓存刷新和错误处理 | 1.5 天 |
| 4 | E9 保存可靠性 | 待提交队列、重试、断点续测、页面关闭提示 | 2 天 |
| 5 | 集成验证 | API 测试、真实文件回归、浏览器 E2E、兼容性检查和文档更新 | 1.5 天 |

预计总量：**8～9 个工作日**。若多人局域网访问被正式纳入，E5 应作为独立前置阶段，不能与普通 v2.1 UI 需求并行掩盖风险。

### 9.7 v2.1 测试方案

1. **解析单测**：纯预检不写文件；功能/场景/矩阵/信息页分类正确；字段歧义和自动补列清单完整。
2. **元数据单测**：旧文件无 `_qoder_runs` 可正常读取；新增、更新、轮次切换不会污染用例列表。
3. **备份单测**：只显示同源备份；恢复前必生成新备份；恢复失败时源文件不变；恢复后解析缓存失效。
4. **保存单测**：网络失败保留草稿；重试成功删除草稿；文件指纹变化返回 409；草稿过期自动清理。
5. **浏览器 E2E**：选择文件 → 预检 → 填活动信息 → 开始测试 → 断网输入 → 恢复网络重试 → 刷新后继续进度 → 恢复旧备份。
6. **真实文件回归**：至少覆盖当前 396 条用例、含图片文件、多结果列文件、含合并单元格文件和子任务文件。

### 9.8 v2.1 完成标准

- 所有正式加载行为在用户确认前不修改源 Excel。
- 任意结果都能关联到测试活动、结果轮次和执行人。
- 写回失败后输入内容不丢失，用户能看到并处理待提交项。
- 用户无需进入 `uploads` 目录即可查看和恢复历史版本。
- `python -m unittest discover tests`、`py_compile`、`node --check` 和浏览器 E2E 全部通过。
- 单机模式下原有 Excel 写回、轮次、筛选、分发合入功能无回退。

### 9.9 v2.1 风险与决策点

- **元数据 Sheet 被外部工具删除**：解析时按“无活动上下文”兼容，不阻断用例执行。
- **Excel 文件被占用**：恢复和写回给出明确关闭文件提示，不尝试强制覆盖。
- **浏览器草稿包含测试现场信息**：默认只保存未提交字段，并提供 TTL 和清理入口。
- **多人访问需求临时扩大**：立即暂停普通 UI 排期，先完成会话隔离、文件锁和版本冲突方案。
